#!/usr/bin/env python3
"""Wrapper script for running hermes-migrate without pip install."""
import sys
from pathlib import Path

# Add repo root to path so hermes_migrate package is importable
sys.path.insert(0, str(Path(__file__).resolve().parent))

from hermes_migrate.cli import main
main()
